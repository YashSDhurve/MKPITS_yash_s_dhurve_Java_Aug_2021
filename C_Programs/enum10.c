//  10  In enumeration, we can define an enumerated data type without the name also.

    #include <stdio.h>  
    enum  {success, fail} status;  
    int main() {  
    status=success;  
    printf("The value of status is %d", status);  
     
        }  

